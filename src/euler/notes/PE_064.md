# [PE_064](../PE_064.java)

The periods length is computed with:
```
private static int length(int n) {
    int a0 = (int) Math.sqrt(n);
    if (a0*a0 == n) return 0;
    int d1 = n - a0*a0;int a1 = 2*a0/d1;
    int m = a1*d1 - a0;
    int d = (n - m*m)/d1;
    int a = (a0 + m)/d;
    int len = 1;
    while (!(m == a0 && d == d1)) {
        len++;
        m = d*a - m;
        d = (n - m*m)/d;
        a = (a0 + m)/d;
    }

    return len;
}
   ```

## Properties
For any positive integer i:
 - $length(x) = 0 \Leftrightarrow x = i \cdot i$
 - $length(x) = 1 \Leftrightarrow x = i \cdot i + 1$

## Results

Some results in the form  
`<L> <time in ms>: <odd period count>`  

<details>
<summary>Results up to L = 10^7</summary>

```
10^1 0 ms: 3
10^2 0 ms: 20
10^3 0 ms: 152
10^4 2 ms: 1322
10^5 56 ms: 11486
10^6 1557 ms: 102662
10^7 44690 ms: 935052
```
</details>